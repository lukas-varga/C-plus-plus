Create a template function that adds two variables of the same data type or concatenates strings.The user has the following options:[1] Adding integer value[2] Adding floating point value[3] Linking a stringAfter selection, the user enters the two values and the function returns the addition/linkage.The result is then output.